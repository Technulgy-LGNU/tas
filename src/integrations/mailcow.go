package integrations

import "tas/src/config"

func CreateMailcowUser(userName string, userEmail string, password string, cfg *config.CFG) error {
	// ToDo: Implement the function to create a user in Mailcow
	return nil
}
